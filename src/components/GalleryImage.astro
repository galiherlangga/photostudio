---
export interface Props {
    src: string;
    alt: string;
}

const { src, alt }: Props = Astro.props;
---

<div>
    <img
        src={src}
        alt={alt}
        class="w-full h-full object-cover"
    />
</div>